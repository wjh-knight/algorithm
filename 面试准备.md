# 多端的小程序的兼容问题，怎么处理

### 1. 条件编译（最核心的手段）

**uni-app 提供了 `#ifdef` / `#endif` 预处理指令，在编译阶段就将平台无关代码剔除**，不会打入最终包体。

配置不同页面路由

根据平台走不同的登录逻辑

> **举例：**
>
> - `pages.json` 里用 `#ifdef MP-WEIXIN` 和 `#ifdef MP-TOUTIAO` 分别**配置不同页面路由**和 tabBar（微信4个tab，抖音3个tab）
> - `App.vue` 里**根据平台走不同的登录逻辑**（微信走 `loginByCode`，抖音走 `loginByCodeDy`）和不同的数据埋点（微信走 `callbackTx`，抖音走 `callbackDy` 做广告转化回传）
> - `utils.js` 里跳转小说插件，微信用 `wx.navigateTo` 跳 `plugin-private://` 协议，抖音用 `tt.navigateTo` 跳普通页面

这样做的**好处是**：**零运行开销**，**不同平台的代码不会互相干扰**。

**配置不同页面路由，根据平台走不同的登录逻辑，页面跳转逻辑不同**

------

### 2. 环境配置分离

`env.js` 通过 `process.env.UNI_PLATFORM` 判断当前编译平台，注入不同的配置：



```js
if (process.env.UNI_PLATFORM === 'mp-weixin') {
    ENVConfig = wxConfig   // appId、baseURL、广告位ID
} else if (process.env.UNI_PLATFORM === 'mp-toutiao') {
    ENVConfig = dyConfig
}
```

同时用 `modifyManifest.js` 在**构建时**动态修改 `manifest.json`，自动注入对应平台的 appid。

**在项目编译的时候调用js脚本，根据不同平台修改manifest.json里的appId**

------

### 3. API 层分离

**不同平台的后端接口前缀和参数不同**，**我采用了独立 API 文件**而不是在同一个文件里写条件判断：

- `apis/novel.js`、`apis/bookshelf.js` → 微信端，`platform=0`，走 `/novel-story/minApp/...`
- `apis/tictok.js` → 抖音端，`platform=1`，走 `/novel-story/dy/...`

公共的接口（如 `user.js`、`pay.js`、`category.js`）则复用。`request.js` **请求头**里带上 `ENVConfig.env`（`wx` 或 `dy`），后端据此区分来源。

------

### 4. 不同平台原生组件分别处理

有些性能敏感或平台特有的 UI，用了原生组件：

- `wxcomponents/` → 微信原生组件（`.wxml` / `.wxss`），比如充值弹窗、全屏组件、段落渲染组件
- `ttcomponents/` → 抖音原生组件（`.ttml` / `.ttss`），比如自定义弹窗

这些组件 API 不兼容，必须分别实现。

------

### 5. 页面/子包/静态资源按平台隔离

- **页面目录**：`pages/tabbar/`（微信）、`pages/tabbarDy/`（抖音），tabbar 数量和功能模块都不同
- **分包**：`pages_A/`（微信子包）、`pages_B/`（抖音子包），抖音多出会员页、阅读记录等页面
- **静态资源**：`static/mp-weixin/`、`static/mp-toutiao/` 各放各的素材

------

6. 遇到的实际兼容问题（加分项，面试可以主动提）

| 问题                                                         | 解决方式                                                     |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| 登录流程差异大（微信 code 换 token 有 401 自动重试，抖音没有） | `request.js` 里 `#ifdef MP-WEIXIN` 单独处理 401              |
| 支付接口完全不同                                             | 微信用 `wx.requestPayment`，抖音用 `tt.pay`，封装了 `dyPayCallback()` 做支付后广告回传 |
| 小说阅读器只在微信有插件生态                                 | 微信用 `requirePlugin` 加载小说插件，抖音自己实现了一个阅读页 `pages/novel_plugin/index` |
| tabBar 图标和数量不同                                        | 分平台目录存放图标，`pages.json` 用条件编译配置不同 tabBar   |

"总的来说，多端兼容的核心思路是：**能用条件编译的就用条件编译（零运行时开销），差异大的就分文件/分目录隔离**。环境配置用 `process.env.UNI_PLATFORM` 统一管理，API 层按平台分文件避免条件判断污染业务代码。关键是**抽象出公共逻辑尽量复用，平台差异的部分收敛到边界处处理**，这样后期维护成本最低。"



# 激励视频广告奖励如果重复发放啊，你怎么处理？

#### 1. `onClose` 回调可能被注册多次

[LeaveReadReward.vue:51-96](vscode-webview://1u70jd7r9mrn9qa22mct0nvvfcfg24l7c75elk2an15v4dgm4tgv/src/components/LeaveReadReward/LeaveReadReward.vue#L51-L96) 里，`initRewardAd()` 每次调用都会执行 `rewardVideoAd.onClose()`。如果组件多次挂载或 `initRewardAd` 被多次调用，`onClose` 里就注册了多个监听器，**广告关一次触发 N 次发奖**。

#### 2. 前端没有防重入的标志位

`onClose` 回调里直接调 `insertIncentiveRecords`，没有 `isRewarded = true` 这样的锁。用户快速操作或者回调意外触发多次时没有保护。

#### 3. 后端接口没有幂等校验

`insertIncentiveRecords` 接口传的是 `incentiveId`（广告位ID）和 `userId`，没有传**唯一请求标识**（如 `requestId` / `transactionId`），后端无法判断是不是同一次广告的重复请求。

#### 4. 组件销毁时没有清理广告对象

[LeaveReadReward.vue:153-160](vscode-webview://1u70jd7r9mrn9qa22mct0nvvfcfg24l7c75elk2an15v4dgm4tgv/src/components/LeaveReadReward/LeaveReadReward.vue#L153-L160) 的 `onUnmounted` 里，`rewardVideoAd.destroy()` 被注释掉了。[transition.vue:33-36](vscode-webview://1u70jd7r9mrn9qa22mct0nvvfcfg24l7c75elk2an15v4dgm4tgv/src/pages_A/transition/transition.vue#L33-L36) 更离谱，用的变量名 `rewardedVideoAd` 跟定义的 `rewardVideoAd` 不一致（**这是个 bug**），destroy 永远不会生效。监听器泄漏会导致旧回调仍然触发。

------

### 三、怎么解决（面试核心得分点）

#### 前端层面



```js
// 1. 加一个发放锁
let isRewarding = false;

rewardVideoAd.onClose(async (res) => {
    if (res && res.isEnded && !isRewarding) {
        isRewarding = true;  // 加锁
        try {
            const result = await insertIncentiveRecords({
                incentiveId: adUnitId,
                userId,
                requestId: generateUUID()  // 唯一请求标识
            });
            if (result.msg === '成功!') {
                uni.showToast({ title: '领取成功！', icon: 'none' });
            }
        } finally {
            isRewarding = false;  // 解锁
        }
    }
});
```



```js
// 2. 组件销毁时一定要清理监听器
onUnmounted(() => {
    if (rewardVideoAd) {
        rewardVideoAd.offClose();  // 移除所有 onClose 监听
        rewardVideoAd.destroy();
        rewardVideoAd = null;
    }
});
```



```js
// 3. 每次 init 前先移除旧监听，防止重复注册
const initRewardAd = () => {
    if (rewardVideoAd) {
        rewardVideoAd.offClose();
        rewardVideoAd.offLoad();
        rewardVideoAd.offError();
        rewardVideoAd.destroy();
    }
    rewardVideoAd = uni.createRewardedVideoAd({ adUnitId });
    rewardVideoAd.onClose(async (res) => { ... });
};
```

#### 后端层面

| 策略               | 具体做法                                                     |
| ------------------ | ------------------------------------------------------------ |
| **幂等校验**       | 用 `userId + incentiveId + 广告回调凭证` 做唯一约束，重复请求直接返回"已发放" |
| **广告回调验证**   | 微信/抖音支持服务端回调验证广告是否真实看完，前端传 `trans_id` 或签名，后端校验后再发奖 |
| **Redis 分布式锁** | `SET key userId:adUnitId NX EX 60`，防止并发重复发放         |
| **数据库唯一索引** | 对 `(user_id, ad_unit_id, created_at)` 加唯一索引，兜底防重  |

------

### 四、总结话术（面试收尾）

> "激励视频防重发的核心思路是：**前端做防守（加锁+清理监听器），后端做兜底（幂等校验+唯一约束）**。不能只依赖前端，因为前端的 JS 可以被绕过，最终要靠后端的幂等性来保证。同时广告平台本身也提供了服务端回调验证机制，应该接入来确保广告确实是被真实看完的。"

# 后台权限系统怎么设计的。 RRBAC 这个这个模型

用户 user 对吧，一个角色对应多个权限，角色 role 权限 permission。

或者说菜单 menu 啊，接口 API 等等这些东西，就是你要把这个模型讲清楚，一个用户他可以绑定多个角色

，对吧？那一个角色是不是可以绑定多个的权限，对不对？那权限呢？又可以细分为接口啊，按钮啊等等这些页面

的某个布局啊等等这些都可以影响。





**"我们后台系统基于 RBAC 模型设计，核心是用户-角色-资源三层结构。**

**资源为MENU 菜单 ，通过 `parentId` 做树形层级。**

**前端这边，登录时后端返回用户的 `token`、`isAdmin`管理员标识和 `resourcesList` 权限列表。我在 路由守卫 `beforeEach` 里做页面级鉴权——遍历权限列表，用资源名和路由 title 做精确匹配；在 侧边栏组件 里用同样的逻辑过滤菜单显示。**

**超级管理员 `isAdmin == 1` 跳过所有检查直接放行。普通用户没权限的页面直接拦住并提示。**

**Token 存在 cookie 里，Pinia 做状态持久化，Axios 拦截器自动注入 token 并处理 401 过期。"**



## 权限变了怎么办？为什么用 name 匹配而不是 path？

| **权限变了怎么办？**                | 重新登录刷新 `resourcesList`。也可以做主动推送，改权限后让后端通知前端清除缓存 |
| ----------------------------------- | ------------------------------------------------------------ |
| **为什么用 name 匹配而不是 path？** | 因为我们前端路由是静态定义的，后端资源的 `name` 和前端路由 `meta.title` 保持一致，**这样匹配更直观，也方便维护** |

# 登录你这个流程是怎么样子的

**我们项目登录的整体流程是这样的：用户访问页面时，路由守卫会先检查 Cookie 里有没有 token，没有的话直接跳转到登录页；登录页有用户名、密码和图形验证码三个字段，验证码的 verKey 是前端随机生成的，调接口拿到 base64 图片展示，用户提交表单时把账号密码和验证码一起 POST 到后端登录接口；登录成功后，后端返回 token、用户信息、权限列表和管理员标识，前端把 token 存到 Cookie（方便跨子域共享），用户信息存到 Pinia 并开启持久化（刷新页面不丢失），用户名存一份 sessionStorage；然后根据 isAdmin 判断角色，超级管理员直接跳首页，普通用户从权限列表里找到第一个有权限的页面跳转过去；后续所有请求通过 axios 请求拦截器自动从 Cookie 读 token 注入到请求头里，响应拦截器监听 401 状态码，过期了就清除登录态跳回登录页；退出登录时清空 Pinia 用户信息、删掉 Cookie 里的 token、跳转到登录页，整个流程就结束了。**

如果面试官追问，可以补充这几点：

1. **Token 存 Cookie 而不是 localStorage**：因为项目有子域共享需求，通过配置 `VITE_COOKIES_DOMAIN` 实现跨子域携带 token
2. **Pinia 持久化**：用 `pinia-plugin-persistedstate` 做了持久化，刷新页面后路由守卫不需要重新请求用户信息就能判断权限
3. **验证码是客户端生成 key、服务端校验**：`verKey` 前端随机生成传给后端，后端生成图片并记住这个 key，提交时前后端对得上就行
4. **权限粒度是页面级**：通过 `resourcesList` 和路由 `meta.title` 匹配来控制页面访问权限

# Vue3   ref reactive computed  watch 的区别

**ref vs reactive**：**它们都是用来创建响应式数据的  `ref` 能定义基本类型，`reactive` 只能定义对象或数组。`computed` 有缓存。**

**computed vs watch**：它们都是**对数据变化的响应**

`reactive` 返回的是**原始对象的 Proxy 代理**。你如果 `state = newObj`，等于把变量指向了新对象，原来 Proxy 的引用就断了，模板里的依赖就追踪不到了。



**第一，ref vs reactive**：它们都是用来**创建响应式数据**的，区别在于：

- `ref` 可以存任何类型，但访问时要 `.value`，底层通过 `RefImpl` 类包装
- `reactive` 只能存对象/数组，**直接访问属性**，底层用 `Proxy` 代理

**第二，computed vs watch**：它们都是对**数据变化的响应**，区别在于：

- `computed` 用于派生新数据，有缓存，依赖不变就不重新计算，必须 return
- `watch` 用于执行副作用，没有缓存，可以拿到新旧值，适合异步操作

**ref 解决类型限制和赋值问题，reactive 解决深层对象访问的便利性**。**基本类型无法被 Proxy 代理**



**computed**

```
计算属性 - 会缓存结果
方法 - 每次访问都会重新计算
缓存优势：适合计算开销大的操作
链式计算：一个计算属性可以依赖另一个计算属性
```

**watch 是监听器。**

**watch 没有缓存，每次变化都执行**

**computed 必须 return 一个值，用于模板渲染；watch 不返回值，用于执行副作用**

**computed 是惰性求值，被读取时才计算；watch 是数据变化时主动执行**

**computed 自动追踪依赖；watch 需要明确指定要监听的数据**



**watchEffect 自动收集依赖、立即执行、不能获取旧值；watch 手动指定依赖、懒执行、能拿到新旧值。简单场景用 watchEffect，需要精确控制用 watch。**

## **总结**

 **computed 计算属性，会缓存结果，必须 返回一个值，用于模版渲染，被读取时才计算，每次访问都会重新计算，自动追踪依赖，每次访问都会重新计算适合计算开销大的操作**

**watch 是监听器。watch 没有缓存，每次变化都执行，watch 是数据变化时主动执行，需要明确指定要监听的数据**



**购物车总价这种计算逻辑，我会用 computed，性能更好。搜索防抖 这种场景，我会用 watch 配合异步请求。**

# axios 请求封装这一块就封装一个 axios 

你首先第一点要讲一下**请求拦截**这个这个你讲了**响应拦截**还有什么还有 **token 的过期**，还有**错误的提示**啊等等，这些东西你都要讲到





**首先我会用 `axios.create()` 创建一个实例，统一配置 `baseURL`、`timeout`、`headers` 等基础参数，这样整个项目所有请求共享一套配置，后续改也只改一处。**

**然后重点是两个拦截器。第一个是请求拦截器 `interceptors.request.use`，在每次请求发出去之前，我会从 `sessionStorage` 里取 token，如果有就挂到请求头的 `token` 字段上，这样后端每次都能拿到身份信息，不需要每个接口单独传；同时如果有公共参数或者请求头统一处理的逻辑，也可以放在这个拦截器里一起做。**

**第二个是响应拦截器 `interceptors.response.use`，这里我做三件事：第一，判断 token 过期的情况，比如后端返回 `code === 401` 或 `4001`，我就清除本地缓存、跳转到登录页、并给用户一个"登录超时请重新登录"的提示；第二，正常情况下直接 `return response.data`，把 axios 多包的那一层解掉，业务代码拿到的就是干净的数据；第三，在错误回调里统一做网络错误的兜底提示，比如超时就提示"网络超时"，断网就提示"网络连接错误"，其他错误统一提示"接口请求失败"，用 `message.error` 给用户弹出来，这样业务层不需要每个接口都自己写一遍错误处理。**

**最后我把 `get` 和 `post` 方法再包一层 Promise 导出，业务侧调用就是 `http.get(url, params)` 这种简洁的写法，所有拦截逻辑、错误处理都走公共的，业务代码只关心成功的结果。"**

# 后台的这个表格页面，你是怎么设计的，对吧？

**后台表格页面一般采用三段式布局：顶部是搜索筛选表单，中间是数据表格，底部或侧边是新增/编辑的弹窗。核心流程是：用户在表单输入条件提交后，把查询参数（含分页 `pageNumber`、`pageSize`）传给后端接口，拿到返回的 `records` 列表和 `total` 总条数，分别赋给表格数据源和分页组件，用户切换页码时重新请求接口刷新表格。表格用 Ant Design Vue 的 `<a-table>` 组件，列定义用 `columns` 数组配置标题、字段和自定义渲染，比如序号列通过 `customRender` 计算全局行号，操作列通过插槽渲染编辑删除按钮。新增和编辑一般用 `<a-modal>` 弹窗或 `<a-drawer>` 抽屉承载表单，提交成功后关闭弹窗并重新拉取表格数据刷新列表。**

# 表格分页请求后的数据太大，导致页面一直没数据怎么处理

加一个loading加载，表格没渲染完成之前显示加载图标



"数据量大导致页面没数据，我会从**三个层面**解决：

1. **请求层**：确保分页参数合理，控制 pageSize，接口只返回必要字段；后端加索引、缓存。
2. **渲染层**：如果单页数据仍然很大，使用**虚拟列表**技术，只渲染可视区域的 DOM。
3. **体验层**：加 loading 状态、请求超时兜底、分页切换防抖，让用户有明确的反馈。

根本原则就是：**后端合理分页 + 前端按需渲染**。"

# 性能优化

你都可以从几个方向去讲，首先第一个分包对吧？

你怎么分包的非核心的这个页面，你把它放放怎么样子的主首页，或者说主包要放呃主要的这样的一个核心要放在

主包里面，资源的优化，我觉得你倒是讲了，然后呢代码的优化。

啊，比如说用这个 减少 setdata 的一个频率啊，然后减少大组件的这个库的一个全量引用啊，对吧，啊，tree 

shaking 啊等等这些东西都是可以减少这个包的大小的嘛。

# 后台图片上传太大怎么处理

beforeUpload

比如说前端的限制是怎么样子的，**文件大小是不是超过限制**啊？是不是**有压缩**？

**文件类型**是不是允许的？这个是你要**排查**的对吧？第二个**后端是不是有所限制**？比如说 **nginx 限制啊，或者说 to**

**ken 签名**啊，或者**说权限是不是过期**了，是不是也要查一下？

然后还有**请求层请求层**你是不是用这个 **formdata 或者说是用 blob** 啊，或者说 **content type** 是不是设置错了？

这就是在网络层你需要去？

### 一、上传前校验

```js
// 文件大小校验
const MAX_SIZE = 5 * 1024 * 1024; // 5MB

function beforeUpload(file) {
  if (file.size > MAX_SIZE) {
    alert('图片不能超过5MB');
    return false;
  }
  return true;
}
```

### 二、前端压缩（核心）

用 Canvas 压缩图片

```js
// 用 Canvas 压缩图片
function compressImage(file, quality = 0.7) {
  return new Promise((resolve) => {
    const reader = new FileReader();
    reader.onload = (e) => {
      const img = new Image();
      img.onload = () => {
        const canvas = document.createElement('canvas');
        canvas.width = img.width;
        canvas.height = img.height;
        const ctx = canvas.getContext('2d');
        ctx.drawImage(img, 0, 0);
        canvas.toBlob((blob) => {
          resolve(blob);
        }, 'image/jpeg', quality);
      };
      img.src = e.target.result;
    };
    reader.readAsDataURL(file);
  });
}
```

### 三、格式转换（PNG → WebP/JPEG）

- WebP 比 PNG 小 **25%~35%**，比 JPEG 小 **25%~34%**
- Canvas 的 `toBlob` 直接支持转 WebP：



```js
canvas.toBlob((blob) => resolve(blob), 'image/webp', 0.8);
```

### 四、分片上传 + 断点续传



```
大文件 → 切成 2MB 一片 → 逐片上传 → 全部上传完后合并
```



```js
function createChunks(file, chunkSize = 2 * 1024 * 1024) {
  const chunks = [];
  let start = 0;
  while (start < file.size) {
    chunks.push(file.slice(start, start + chunkSize));
    start += chunkSize;
  }
  return chunks;
}

// 逐片上传，记录进度，支持断点续传
async function uploadChunks(file) {
  const chunks = createChunks(file);
  for (let i = 0; i < chunks.length; i++) {
    const formData = new FormData();
    formData.append('chunk', chunks[i]);
    formData.append('index', i);
    formData.append('total', chunks.length);
    await axios.post('/upload/chunk', formData);
  }
}
```

### 五、秒传

```js
// 上传前计算文件 hash（用 spark-md5 库）
import SparkMD5 from 'spark-md5';

async function getFileHash(file) {
  return new Promise((resolve) => {
    const reader = new FileReader();
    reader.onload = (e) => {
      const spark = new SparkMD5.ArrayBuffer();
      spark.append(e.target.result);
      resolve(spark.end());
    };
    reader.readAsArrayBuffer(file);
  });
}

// 先查后传
const hash = await getFileHash(file);
const res = await axios.get(`/upload/check?hash=${hash}`);
if (res.data.exists) {
  // 秒传，直接返回已有 URL
  return res.data.url;
}
```

### 六、懒加载

- 图片 URL 拼接 OSS 的缩略图参数：`?x-oss-process=image/resize,w_300`
- 列表页用小图，点击/详情页才加载大图
- `loading="lazy"` 原生懒加载

------

### 面试回答模板

> 前端主要从 **三个层面** 处理：
>
> **第一，上传前**：校验文件大小，超过限制直接拦截，同时用 **Canvas 压缩图片**，也可以转成 WebP 格式，体积能减少 30% 左右。
>
> **第二，上传中**：大文件采用**分片上传**，切成 2MB 一片逐个上传，配合 **断点续传**，网络中断可以从断点继续。上传前先算文件的 **MD5 hash**，如果服务器已存在就**秒传**，避免重复上传。
>
> **第三，展示时**：列表页加载**缩略图**，详情页再加载原图，配合**懒加载**减少首屏带宽。



前端层面：**首先在上传前做大小校验，超限直接拦截提示**；其次用 Canvas 或 compressorjs 对图片进行**质量和尺寸压缩**，支持 WebP 转换。如果图片仍然很大，就用**分片上传 + 断点续传**，配合文件哈希实现秒传。服务端也要同步调大上传限制，推荐用 OSS 直传避免经过应用服务器。





# 路由权限刷新后白屏

刷新白屏对，就是这个题目呢，其实很简单，就是因为你你在刷新的时候你要**去判断是不是有 token**。

如果有 token 的话，但是呢，这个用户信息啊，或者说权限啊，它没有加载你这时候你需要去重新的去请

求一下用户的信息。

然后根据后端的这个返回的这个用户信息去动态的生成这个这个路由，然后把它放到这个路由里面去，然后才能够

去进行加载。



**刷新白屏的本质是"有 Token 但动态路由丢了"，解决办法是在路由守卫里判断：有 Token 没权限 → 重新请求用户信息 → 动态 addRoutes → next({ ...to })**

# pinia它这个数据持久化，它很容易导致这个用户看到这个旧的数据，但是没有及时刷新

很有可能就是前端持久化这个旧的权限，但是没有及时刷新，导致拿到旧的数据，那这种情况下面你就登录成功必

输，重新去拉取这个用户的一个权限和信息。

并且对于这种 local storage 啊，或者说这个 session storage 啊，这个这个数据啊，**不能够长期去信任**，并且用户这个**退出登录的时候，你是不是要把这个浏览器的缓存啊，或者批量要进行一个清空**，对吧？





Pinia 持久化本质上就是 **`localStorage` 的封装**，**过期数据是缓存系统的固有问题，不是 Pinia 独有的**。解决方案和其他缓存场景一样：

1. **加 TTL 过期机制**
2. **stale-while-revalidate**：先展示缓存，后台静默刷新
3. **精细化控制**：只持久化 UI 偏好类数据，业务数据每次请求
4. **版本号兜底**：发版时强制清除旧缓存

#  view 3重复接口的一个问题，这是一个非常经典的一个问题。

**On mounted 它调用了一次 watch 是不是会立即执行一次？**

因为你 watch 设计了那个 immediate 对吧，所以这两个会容易导致对不对？它有副组件和子组件，是不是可

能会同时请求同一个接口，是不是也有可能？





"这个问题需要分场景来处理：

**第一层，UI 层**：按钮加 `loading` 状态，请求期间禁用按钮，防止用户重复点击。这是最基础的做法。

**第二层，请求层**：用 `AbortController` 取消上一次未完成的请求。适合搜索框这种'只关心最后一次结果'的场景。

**第三层，拦截器层**：在 axios 拦截器中维护一个 `pendingMap`，用 `method + url + params + data` 作为 key，相同请求直接取消前一个。这是全局方案，不侵入业务代码。

**第四层，业务层**：特定场景用**防抖节流**，比如搜索用 debounce，滚动加载用 throttle。

实际项目中通常是组合使用——**按钮 loading + 拦截器去重** 基本能覆盖大部分场景。"

# keepalive

在 Vue 3 中，`<KeepAlive>` 是一个内置组件，主要用于**缓存动态组件或路由组件**，**避免重复初始化带来的性能损耗**。

**核心用法：** 配合 `router-view` 使用，并通过 `include` / `exclude` 控制哪些组件被缓存，`max` 限制最大缓存数量。

**生命周期：** 被缓存的组件会多出 **`onActivated`** 和 **`onDeactivated`** 两个钩子，分别对应组件**激活和停用**时的业务逻辑（比如重新拉取数据或暂停定时器）。

**内部原理：** **基于缓存容器存储组件实例，采用 LRU 淘汰策略，复用实例时跳过创建和挂载流程，只执行激活步骤。**

**注意事项：** 不是所有组件都适合缓存，需要根据业务场景决定，比如实时性要求高的页面不应该缓存。”

#  ts 的问题

# 如果你是一个组长，给你一个项目怎么办

### 一、需求阶段 — 搞清楚做什么

- **和产品/需求方充分沟通**，明确需求边界、优先级、交付时间
- 产出 **需求评审文档**，识别模糊点、不合理点，主动提出疑问
- **不要闷头就干**，先确认再动手

------

### 二、技术选型阶段 — 怎么做

- 根据项目规模、团队技术栈、性能要求做

  技术选型

  - 框架：Vue / React？
  - 构建：Vite / Webpack？
  - UI 库：Element Plus / Ant Design？
  - 状态管理、路由、HTTP 库等

- 评估是否需要引入**新方案**（SSR、微前端等），不能为了用而用

- 产出 **技术方案文档**，和团队成员讨论确认

------

### 三、架构设计阶段 — 怎么搭

- **目录结构设计**：按功能/模块划分，约定命名规范

- **公共组件抽取**：识别通用 UI 和逻辑，避免重复造轮子

- 封装基础能力

  ：

  - 请求拦截、统一错误处理
  - 路由权限控制
  - 公共工具函数/hooks

- **制定代码规范**：ESLint、Prettier、Git 提交规范（commitlint）

------

### 四、任务拆分 & 分配 — 谁做什么

- 将需求**拆成可执行的任务**，估时
- 根据成员能力合理分配：
  - 核心/难点模块 → 经验丰富的成员
  - 常规页面 → 普通成员 / 新人
- 使用**项目管理工具**（Jira、飞书、禅道）跟踪进度
- 明确 **每个人的责任和截止时间**

------

### 五、开发阶段 — 保质量、控进度

| 关注点       | 做法                                             |
| ------------ | ------------------------------------------------ |
| **代码质量** | Code Review，合并前必须 review                   |
| **进度把控** | 每日站会/周会，及时发现阻塞                      |
| **技术难点** | 组长攻坚或协调资源，不让成员卡住                 |
| **分支管理** | Git Flow / 功能分支，保护主分支                  |
| **联调沟通** | 提前和后端约定接口文档（Swagger/YApi），减少返工 |

------

### 六、测试 & 上线阶段 — 兜底

- 开发完成后先**自测**，再提测给 QA
- 配合测试修 Bug，**区分优先级**（P0 必须当天修）
- 上线前做 **回归测试 + 冒烟测试**
- 准备**回滚方案**，万一出问题能快速恢复
- 上线后关注**线上监控**（错误日志、性能指标）

------

### 七、复盘阶段 — 持续改进

- 项目结束后组织**复盘会议**
- 总结：
  - 哪些做得好？→ 沉淀为团队规范
  - 哪些踩了坑？→ 形成文档避免重复
  - 流程有什么可以优化的？
- 输出 **项目总结文档**

------

## 面试推荐回答模板

> 我会分几个阶段来推进：
>
> **第一**，先和产品对齐需求，确认范围和时间节点，不明确的地方主动提出来；
>
> **第二**，做技术选型和架构设计，定好目录结构、公共组件、代码规范，打好基础；
>
> **第三**，把需求拆成任务，根据成员能力合理分配，用工具跟踪进度；
>
> **第四**，开发阶段坚持 Code Review，每日同步进度，遇到阻塞及时解决；
>
> **第五**，上线前充分测试，准备回滚方案，上线后关注线上稳定性；
>
> **最后**，项目结束后复盘，把经验沉淀下来，持续优化团队流程。
>
> 作为组长，我的角色不只是写代码，更重要的是**把控方向、协调资源、保证交付质量**。

------

## 面试加分点

- 提到 **"主动沟通"** — 和产品、后端、测试都要保持信息同步
- 提到 **"风险预判"** — 提前识别可能延期的点，留 buffer
- 提到 **"培养成员"** — 给新人成长机会，而不是什么都自己干
- 提到 **"文档意识"** — 技术方案、接口文档、部署文档都要写

核心思想：**组长的价值不是自己写得最多，而是让团队整体高效交付高质量产品。**



我拿到一个项目之后，一般会先跟产品那边**对一下需求**。不是说他给我个文档我就直接开始干了，我会先过一遍，看看有没有逻辑上不通的地方，或者没说清楚的地方，提前问清楚。因为**需求阶段搞明白了，后面能省很多返工的时间**。

需求确认之后，我会先做一下**技术方案**。根据项目大小、团队成员的技术栈，决定用什么框架、用什么 UI 库、目录结构怎么组织。这些定下来之后我会跟组员开个短会说一下，大家有意见可以提，但最终要有个统一的方案，不能各写各的。

然后就是**拆任务、排期**。我会把需求拆成一个个小的功能点，根据每个人的能力去分配。比如比较难的模块，像权限控制、复杂表单这种，我会分给经验比较丰富的人。一些常规页面就给其他同学做，也当是给他们锻炼的机会。

开发过程中我会**每天花几分钟同步一下进度**，不用搞很正式的站会，群里说一下就行。谁遇到阻塞了、接口还没好、或者有技术问题，我能解决的当场解决，解决不了的我去找后端或者其他人协调。

代码层面我会要求大家**提交之前做测试**。不需要每行都看，但核心逻辑一定要过一遍，一方面保证质量，另一方面也是让组员之间互相学习。

我会**上线之前再跑一遍主流程**，确认没问题再提测。上线的时候我会准备一个回滚方案，万一出了问题能第一时间恢复，不至于手忙脚乱。

上线之后我会关注一下**线上有没有报错**，比如看看 Sentry 或者日志，有异常及时处理。

最后项目结束了，我会带大家**开个复盘会**，聊聊这次哪些地方做得好、哪些地方踩了坑。踩过的坑我会整理成文档，下次新项目就能避免。





## 追问：如果组员不服你怎么办？

> 我觉得靠**能力服人**是最直接的。遇到技术难题我能顶上去，大家自然会认可你。另外就是**分配任务要公平**，不能说脏活累活都给一个人干。平时多帮别人解决实际问题，关系搞好了，管理自然就顺了。

## 追问：项目要延期了怎么办？

> 首先我会**评估一下到底延期多久**，是几个功能点来不及还是整体来不及。如果只是局部问题，我会调整一下分工，让进度快的人支援一下。如果整体来不及，我会**提前跟产品和领导沟通**，能砍需求的砍需求，能延的延。最怕的是最后一天才说做不完，那就很被动了。





**排查、本地复现、定位、回滚、热更新、首屏加载、白屏时间、懒加载、预加载、回流（重排）、重绘、内存泄露、重构、封装、复用、**

**崩溃、卡顿、闪退、白屏、兼容、联调、自测、发布、回滚、监控、**